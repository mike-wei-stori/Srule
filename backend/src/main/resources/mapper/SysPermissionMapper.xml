<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stori.rule.mapper.SysPermissionMapper">

    <select id="selectByCode" resultType="com.stori.rule.entity.SysPermission">
        SELECT * FROM sys_permission WHERE code = #{code}
    </select>

    <select id="selectByRoleId" resultType="com.stori.rule.entity.SysPermission">
        SELECT p.* FROM sys_permission p 
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id 
        WHERE rp.role_id = #{roleId}
    </select>

    <select id="selectByUserId" resultType="com.stori.rule.entity.SysPermission">
        SELECT p.* FROM sys_permission p 
        INNER JOIN sys_role_permission rp ON p.id = rp.permission_id 
        INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id 
        WHERE ur.user_id = #{userId}
    </select>

</mapper>

